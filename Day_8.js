const str = "Hello, World!";


// Returns the character at the specified index.
const value = str.charAt(0);
console.log(value);

console.log(str);

const str1 = ["Hello", "World"];


// join() method is used to concatenate the array elements
// with a specified separator and returns the result as a string.


const newJoinString = str1.join();

console.log(newJoinString);

